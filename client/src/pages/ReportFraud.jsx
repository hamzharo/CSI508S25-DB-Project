// src/pages/ReportFraud.jsx
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'; // Assuming you use Shadcn

const ReportFraud = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Report Fraud</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Fraud reporting form will be here.</p>
        {/* Add form elements later */}
      </CardContent>
    </Card>
  );
};

export default ReportFraud;